#!/usr/bin/env bash
# Run the test suites before anything leaves this machine. ~19s.
#
# Too slow for every commit, fast enough for a push, and a push is the point
# where a regression stops being yours alone.
#
# Bypass with: git push --no-verify
exec "$(git rev-parse --show-toplevel)/tests/run.sh"
